# Description: session configuration for JobRad Loop Nightwing development

cd ~/new-dev/nightwing

# Main dev tab
layout tall
enabled_layouts tall:bias=70;full_size=1
launch --title "Helix Editor" --cwd=current hx .

# Create lazygit tab
new_tab Lazygit

cd ~/new-dev/nightwing

launch --title "Lazygit" --cwd=current lazygit
